export const enum AppRoutes {
    SIGN_IN = "/",
    SIGN_UP = "/signup",
    MAIN = "/main",
    ORDER = "/order",
    STOCK = "/stock",
    SEARCH = "/search",
    MY_ASSET = "/my-asset",
    HISTORY = "/history",
    TOKEN = "/token",
}